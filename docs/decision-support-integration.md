# Рекомендации и песочница: инструкция по интеграции

## Назначение ветки

Ветка добавляет интерактивные UI-заглушки для двух разделов поверх существующего мониторинга. Реальные оркестратор, агентный контур, сценарный оптимизатор и LLM намеренно не подключаются. Текущий endpoint `POST /api/datasets/{id}/scenario` остаётся источником расчёта для демонстрационной песочницы.

## Точки интеграции

- `frontend/src/App.tsx` владеет навигацией, выбранным датасетом и моментом `to`.
- `frontend/src/views/DecisionSupportView.tsx` остаётся единым внешним контейнером для двух режимов.
- `frontend/src/demo/decisionSupportDemo.ts` содержит только временные presentation fixtures.
- `frontend/src/api.ts` и `frontend/src/types.ts` сохраняют существующий контракт `ScenarioRequest`/`ScenarioResult`.
- `backend/app.py` и `backend/scenarios.py` не требуют изменений для UI-заглушек.

## Поток данных

Мониторинг выбирает датасет и момент. Рекомендации принимают последний момент либо период, показывают последовательность pipeline 0→6 и итоговую текстовую рекомендацию. Действие «Открыть в песочнице» передаёт контекст через callback и переводит пользователя на тот же `DecisionSupportView` в sandbox-режим. Песочница загружает demo-строку, позволяет менять параметры и отправляет совместимый `ScenarioRequest` в существующий scenario API.

Будущий API должен заменить только demo-adapter и не менять внешний navigation contract:

```ts
interface RecommendationContext {
  datasetId: string;
  from?: string;
  to: string;
  mode: "latest" | "period";
  source: "recommendation" | "monitoring" | "manual";
  recommendationId?: string;
  scenarioId?: string;
}
```

## UX-контракт

Рекомендации поддерживают режимы «Последние данные» и «Период», показывают качество входа, статусы очистки/мониторинга/оркестратора, 10→6 сценариев, итог, риски, стоимость, уверенность и допущения. Результат никогда не является операторской командой.

Песочница сначала предлагает готовую строку датасета, затем ручной ввод. После выбора показываются качество сырья, P8/T11/F19, резервуары, присадка, цели, горизонт и шаг. Результат содержит сравнение до/после, траекторию серы, риски, стоимость и допущения. При входе из рекомендации должен быть виден контекст «Загружена рекомендация».

## Правила merge

1. Сохранить общую оболочку `App.tsx` и state мониторинга.
2. Не переименовывать и не ломать `ScenarioRequest`/`ScenarioResult`.
3. Подключать реальные pipeline endpoints через отдельный adapter вместо замены demo fixtures внутри представления.
4. При конфликте в `App.tsx` сохранить callback перехода в sandbox и существующий `datasetId`/`to`.
5. После подключения backend удалить demo-источник только после покрытия состояний loading, error, empty и partial data.

## Проверка после объединения

Запустить `npm run typecheck`, `npm run build` и frontend-тесты. Вручную проверить latest/period, переход рекомендация→песочница, выбор строки, ручной ввод, сброс, пересчёт, пустой датасет, предупреждения и mobile layout.

## Ограничения

Линейная модель сценариев прозрачна и редактируема, но не является промышленным оптимизатором. Demo-статусы не подтверждают фактическое выполнение оркестратора, агентов или LLM.
