# Технологический процесс и текстовое представление схем

Этот документ переносит в текст полезную информацию из `materials/АВТ_схемы.pdf`, четырёх страниц `materials/Updates/Ustanovka_AVT_merged.pdf` и трёх обновлённых одностраничных схем с рукописными короткими тегами. Для точного значения и единицы каждого короткого кода используйте [справочник тегов](tag_dictionary.md).

## Общая цепочка

```text
Сырая нефть
  -> подготовка/ЭЛОУ и теплообмен
  -> печь и атмосферная часть АВТ
  -> прямогонная дизельная фракция
  -> промежуточный резервуарный парк (несколько часов, максимум около суток)
  -> смешение дизельной фракции с водородсодержащим газом
  -> нагрев
  -> реактор Р-202 гидроочистки
  -> стабилизация
  -> гидроочищенный дизельный компонент
  -> сценарный блендинг с керосином, газойлем и присадками
  -> товарный резервуар и контроль качества
```

АВТ также выдаёт лёгкие фракции и тяжёлый остаток/мазут. Вакуумная часть выделяет фракции до 350 °C, 350–560 °C, тяжёлые потоки и гудрон. Эти боковые потоки важны для ВАК АВТ, но не все идут в рассматриваемую цепочку товарного дизеля.

## Обновлённая упрощённая схема, страница 1 — АВТ

Входные характеристики сырой нефти:

- `crude_feed_rate_tph` — суммарный расход нефти;
- `crude_density_kgm3` — плотность;
- `crude_sulfur_wt_pct` — сера в нефти.

Нефть нагревается в печи АТ и поступает в атмосферную колонну. Управляемое воздействие обозначено как `avt_furnace_outlet_temp_c`. На колонне показаны `avt_column_top_temp_c`, `avt_column_pressure_mpa_abs` и `avt_diesel_flow_tph`. Выходы: лёгкие фракции, прямогонная дизельная фракция и мазут. Прямогонный дизель направляется на гидроочистку.

## Обновлённая упрощённая схема, страница 2 — гидроочистка

Входы: дизельная фракция с АВТ и водородсодержащий газ. Показаны:

- `ht_feed_flow_m3h` — объёмный расход сырья;
- `ht_h2_purity_mol_pct` — чистота водорода;
- лабораторные `ht_sulfur_in_mgkg` и `ht_nitrogen_in_mgkg`;
- управляемые `ht_inlet_temp_c`, `ht_gas_oil_ratio_m3m3`, `ht_pressure_mpa`;
- состояние реактора `ht_bed_mean_temp_c`, `ht_pressure_mpa`, `ht_delta_pressure_kpa`;
- ключевое качество `ht_sulfur_out_mgkg`.

После смешения и нагрева поток проходит реактор с катализатором. Выходы — отходящие газы и очищенный дизельный компонент. В актуальном коротком справочнике ближайшие прямые соответствия: объёмный расход сырья `F15`, температура входа `T6`, давление входа `P13`. Газ/сырьё следует рассчитывать из согласованных газовых и сырьевых расходов; отдельной колонки с таким названием нет.

## Обновлённая упрощённая схема, страница 3 — блендинг

Три модельных компонента: очищенный дизель, керосин и газойль. Для каждого задаются доля `blend_component_share_pct` и запас `blend_component_stock_t`. Есть две модельные присадки с полями `additive_type` и `additive_dose_kg_t`.

Управляемые переменные — доли компонентов, выбор и дозировка присадок. Сумма долей должна быть 100%. На входе в расчёт нужны свойства компонентов: плотность, сера, цетановое число и T95. Выходные поля: `product_sulfur_mgkg`, `product_density_kgm3`, `product_cetane`, `product_t95_c`, `product_cfpp_c`.

Это сценарная схема. Организаторы прямо сообщили, что реальных рецептур, расходов дозирования, запасов и истории блендинга не предоставят.

## Обновлённая упрощённая схема, страница 4 — вся цепочка

На АВТ управляются температура печи и давление колонны; на гидроочистке — температура, давление и расходы; на блендинге — доли и дозировка. В товарном резервуаре указаны ограничения:

- сера ≤ 10 мг/кг;
- T95 ≤ 360 °C;
- цетановое число ≥ 51.

## Детальная схема 1 — К-1 и подготовка нефти

Сырая нефть проходит параллельные теплообменные ходы, ЭЛОУ и ещё один блок теплообмена, затем поступает в К-1. Низ/циркуляционный поток связан с печью П-1/1; верх К-1 охлаждается, конденсируется в Е-1 и возвращается орошением. Пар подаётся в нижнюю часть К-1. Из низа часть потока направляется к печам П-1/2 и П-1/3.

Рукописно нанесённые позиции:

- `F7`, `F8`, `F9` — три хода расхода обессоленной нефти;
- `D10` — плотность нефти на подаче;
- `T1` — температура верха К-1;
- `P2` — давление бензиновых паров верха К-1;
- `F3` — расход бензина на орошение К-1;
- `P4` — давление низа К-1 в Е-6;
- `F5` — расход пара в К-1;
- `T6` — температура низа К-1 и зелёный контур печного регулирования.

На исходной схеме дополнительно нанесены длинные англоязычные aliases (`crude_feed_rate_tph`, `crude_density_kgm3`, `crude_sulfur_wt_pct`, `avt_column_top_temp_c`, `avt_column_pressure_mpa_abs`, `avt_furnace_outlet_temp_c`). Они описывают модельный смысл, а не отдельные колонки CSV.

## Детальная схема 2 — К-2 и атмосферная ректификация

В К-2 поступает отбензиненная нефть после печей П-1/2 и П-1/3. Есть три циркуляционных орошения и боковые отпарные колонны К-6, К-7, К-9. Основные выходы: нафта на стабилизацию, бензин 120–180 °C, керосин 150–250 °C, дизельные фракции 240–290 и 290–350 °C, мазут.

Группировка позиций по узлам:

- верх К-2: `T20`, давления `P21`, `P22`, `P23`, дополнительное `P67`, расход острого орошения `F19`;
- 1-е циркуляционное орошение: `T13`, `F14`, возврат `T18`;
- 2-е циркуляционное орошение: `F12`, возврат `T17`;
- 3-е циркуляционное орошение: `T11`, `F64`, возврат `T15`;
- К-6/бензин: `T24`, пар `F26`, выход `F25`;
- К-7/керосин: пар `F27`, выход `F34`;
- К-9/дизель: температура перетока `T66`, пар `F28`, выходы `F32` (240–290) и `F30` (290–350), массовый расход 290–350 `W70`, температура продукта `T71`;
- низ К-2: `T33`, пар `F29`, производительность по отбензиненной нефти `F65`, поток в П-3 `F31`.

Лабораторные точки АВТ 1, 2, 2.1 и 3 — разные физические места по ходу установки, а не разные времена одной точки. Эксперт назвал точку 3 конечным выходом одного из потоков, но однозначной таблицы «номер точки ЛИМС → конкретная линия схемы/фракция ВАК» не предоставлено.

## Детальная схема 3 — К-10 и вакуумная часть

Мазут из К-2 нагревается в П-3 и поступает в вакуумную колонну К-10. Верх соединён с Е-15 и барометрическим ящиком. Боковые циркуляции и насосные орошения формируют фракции до 350 °C, 350–560 °C, 420–500 °C и тяжёлые потоки. Низ даёт гудрон и направления на битумную установку/УПБ.

Группировка позиций:

- верх К-10: `T49`, вакуум `P50`, пересчёт вакуума `P51`;
- верхнее и среднее циркуляционные орошения: `F35`, `F36`, `T37`, `T38`, `T39`, `T40`, `F41`;
- фракция 420–500: `T42`, объёмный расход `F59`;
- уровень и перепады: `L43`, `P44`, `P52`;
- НЦО/фракция 350–560: `F46`, `T47`, `F53`, `F54`, `F60`, `T61`, `F62`;
- печь П-3: поток `F31`, температура выхода `T55`;
- фракция до 350: реальный `F56`, виртуальный `F57`, температура `T58`;
- гудрон: реальный `F68`, виртуальный `F69`, направление на битумную установку `F63`;
- дополнительные тяжёлые потоки: `F45`, `F64`, `F65` участвуют в формулах ВАК согласно справочнику.

Рукописные позиции на схемах местами перекрывают символы и не всегда однозначно указывают сторону клапана. Поэтому физический смысл и единицы берутся из обновлённого XLSX, а PDF используется для топологии и расположения на аппарате.
